<?php

// The vendor directory
define('VENDOR',		dirname(dirname(__DIR__)) . '/vendor/');

/**
 * Modules,
 * Defining a module and its path here will load it into the application. 
 * You can load modules from composer, just add composer => true, and it will
 * get the vendor directory prepended to its location.
 *
 */
Return Array(



);

